<div class="container">
        <div class="form">
            <div class="form-header">
                <p>タスク登録</p>
            </div>
            
            <div class="form-main">
            <%= form_tag("/tasks/create") do %>
                <p></p>
                <textarea class="task_text_box" type="text" name="context" value="<%= @task.context %>"></textarea>
                <input class="btn" type="submit" value="登録">
            <% end %>
            </div>
        </div>
    </div>